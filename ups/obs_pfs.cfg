# -*- python -*-

import lsst.sconsUtils

dependencies = {
    "required": ["utils", "afw", "daf_butlerUtils", "obs_subaru"],
    "buildRequired": ["swig"],
}


config = lsst.sconsUtils.Configuration(
    __file__,
    hasDoxygenInclude=False,
    hasSwigFiles=True,
)
